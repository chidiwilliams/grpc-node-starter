syntax = "proto3";

package User;

service User {
  // Officers
  // rpc createOfficer(officerInfo) returns(res) {};
  // rpc updateOfficer(updateInfo) returns(res) {};
  // rpc deleteOfficerByID(onlyID) returns(res) {};
  // rpc getAllOfficers(emptyReq) returns(res) {};
  // rpc getOfficerByID(onlyID) returns(res) {};
  // rpc authenticateOfficer(loginDets) returns(res) {};
}

message res {
  optional bool success = 1;
  optional string data = 2;
  optional string message = 3;
}
